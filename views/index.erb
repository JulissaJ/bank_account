<!DOCTYPE html>
<html>
  <head>
    <title>Index</title>
    <link rel="stylesheet" href="style.css" />
  </head>

  <body id='home'>
    <h1>Accounts</h1>
    <% @accounts.each do |acc| %>
      <p><a href=<%= 'accounts/' + URI::encode(acc)%>><%= "#{acc}" %></a></p>
    <% end %>
  </body>
</html>
